# Data

All repository data is synthetic.

- `sample/` can contain small illustrative extracts.
- `generated/` is created by the pipeline and contains the full synthetic dataset plus the local SQLite warehouse.
- The synthetic ground-truth file exists only to evaluate whether the detection system can recover intentionally injected review scenarios.

Do not replace these files with real PHI or proprietary payer data.
